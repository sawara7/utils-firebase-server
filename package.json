{
  "name": "utils-firebase-server",
  "version": "0.0.5",
  "author": "sawara7",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncu -u && npm install && npm update && tsc",
    "commit": "git commit -am 'commit' && npm version patch && git push"
  },
  "bundleDependencies": [],
  "dependencies": {
    "firebase-admin": "^12.2.0",
    "utils-mongodb": "github:sawara7/utils-mongodb"
  },
  "devDependencies": {
    "@types/node": "^20.14.11",
    "typescript": "^5.5.3"
  }
}
